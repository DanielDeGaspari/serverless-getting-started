// A model for match with params: playerList and time
class Match {

    /**
     * @param {*} playerList A list of players
     * @param {*} time timestamp
     */
    // this.paramName = paramName;
    constructor(playerList, time) {
    }

    getPlayerList() {
    }

    getTime() {
    }
}

module.exports = Match;